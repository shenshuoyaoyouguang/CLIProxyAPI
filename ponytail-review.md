# Ponytail Review: CLIProxyAPI main vs origin/main

246 files, +27582/-2870 lines. Scanned top-impact files in `sdk/cliproxy/auth/`, `sdk/proxyutil/`, `internal/logging/`, `internal/registry/`, `internal/runtime/executor/`, `sdk/cliproxy/executionregistry/`, and `sdk/cliproxy/`. Findings ordered by net deletability.

L1. `sdk/cliproxy/auth/quota_signals.go:127`: `quotaSignalRetentionRank` 11-line `switch` ranking headers with magic ints 0–6. Replace with a tiny `case "x-codex-": return 4` after the prefix-specific cases and a single `return 99` default. ~6 lines.
L2. `sdk/cliproxy/auth/quota_signals.go:166`: `isQuotaSignalHeaderForProvider` 30 lines of nested prefix matches + 8-element `marker` slice (`Contains`) for "x-codex-additional-*". Use a single `strings.HasPrefix(name, "x-codex-")` and a per-prefix table of optional marker lists. ~15 lines.
L3. `sdk/cliproxy/auth/quota_signals.go:200`: `mergeQuotaObservation` is dead code (newer observation always replaces; nothing in the package calls it). Delete entirely. -12 lines.
L4. `sdk/cliproxy/auth/quota_signals.go:75` (`cooldownFieldsOf`) and `sdk/cliproxy/auth/quota_signals.go:85` (`applyCooldownFields`): hand-rolled 4-field copies of `QuotaState`. Replace with `dst.Exceeded, dst.Reason, … = src.Exceeded, …` inline at the 1–2 callers; delete the helpers. -20 lines.
L5. `sdk/cliproxy/auth/cooldown_state.go:115` (`FileCooldownStateStore.Save`): `os.MkdirAll(s.dir, 0o700)` then `os.MkdirAll(dir, 0o700)` per-path — second call is the per-file directory. Per-auth subdirs are dead; one shared dir suffices. ~5 lines.
L6. `sdk/cliproxy/auth/cooldown_state.go:1` whole file: hand-rolled `.cds` per-auth state files. Cooldown is already in-memory; the persistence layer just needs `if records == nil { remove }` + `os.WriteFile`. Whole file collapses to ~30 lines vs 270. -180 lines.
L7. `sdk/cliproxy/auth/auto_refresh_loop.go:1`: full `container/heap` implementation plus 4 helper maps for "due-time heap with dirty-set". `time.AfterFunc` per auth or a sorted slice + `time.Ticker(1s)` covers the same surface in ~40 lines. -200 lines.
L8. `sdk/cliproxy/auth/auto_refresh_loop.go:79` (`worker` + `run`): 16-goroutine worker pool to refresh ~N auths. `for { time.Sleep(interval); for _, a := range auths { refresh(a) } }` is the same code. -40 lines.
L9. `sdk/cliproxy/auth/home_session_alias.go:1`: LRU-style alias cache with `container/list` for eviction. The "session alias" concept is one `map[canonical][]alias`; no LRU needed at 4096 entries. ~80 lines collapse to ~25. -55 lines.
L10. `sdk/cliproxy/auth/session_cache.go:1`: same shape as L9 with a goroutine `cleanupLoop` that wakes on a never-closed channel. `sync.Map` + lazy `Get` cleanup is enough. ~150 lines collapse to ~40. -110 lines.
L11. `sdk/cliproxy/auth/home_in_flight_publisher.go:99` (`StartHomeInFlightPublisher`): 4-tier `encodeHomeInFlightFreeze` (aggregate groups + details + parts + overflow) for "send a JSON snapshot". Stdlib `encoding/json` of one struct, then `http.Post`. ~150 lines collapse to ~30. -120 lines.
L12. `sdk/cliproxy/auth/scheduler.go:1` whole file: 1000-line reimplementation of `RoundRobinSelector`/`WeightedRoundRobinSelector` with a 4-level "readyBucket → readyView → cooldownQueue" tree. The existing `Selector` interface in `selector.go` already does this. Delete the whole file and reuse `selector.go`. -1000 lines.
L13. `sdk/cliproxy/auth/scheduler.go:42` (`smoothWeightedState`) and 5 cursor-snapshot/restore helpers: `current/weights` map-of-int64 mirrors of `WeightedRoundRobinSelector` state. Just delegate to the existing selector. -60 lines.
L14. `sdk/cliproxy/auth/conductor.go:90` (`Manager` struct): 17 mutex/lock/atomic fields, 7 of them `sync.Mutex`-only for code paths with 1 caller. `selectorMu`, `configCooldownMu`, `requestPrepareLocks`, `refreshLocks` are all single-purpose locks guarding 1-line critical sections. One `mu` per existing concern; drop the rest. ~30 lines.
L15. `sdk/cliproxy/auth/conductor.go:1` (`ProviderExecutor` interface, `RequestAuthPreparer`, `ExecutionSessionCloser`, `Hook`, `NoopHook`, `Selector`, `StoppableSelector`, `PluginScheduler`): 8 interfaces where each has 1 implementation in the same package. YAGNI: define a single `Executor` struct and pass closures. -120 lines.
L16. `sdk/cliproxy/auth/oauth_model_alias.go:153` (`resolveModelAliasPoolFromConfigModels`): 40-line double loop doing two passes (alias-equals-candidate, then name-equals-candidate) with a `seen` map. One pass with both checks inside the loop. -20 lines.
L17. `sdk/cliproxy/auth/api_key_model_capabilities.go:1`: parallel `apiKeyModelCapabilityTable` mirroring `apiKeyModelAliasTable`. Both compiled in the same `rebuildAPIKeyModelAliasFromRuntimeConfig` switch. Fold into a single `apiKeyModelRoutes` struct; one map, one build. -120 lines.
L18. `sdk/cliproxy/auth/api_key_model_capabilities.go:64` (`CodexAPIKeyModelIsCompat`): per-model name/alias loop with `ParseSuffix` and 4-way `EqualFold`. Two `map[name/alias]Model` lookups + a 2-line `IsCompat` getter. -30 lines.
L19. `sdk/cliproxy/auth/conductor_models.go:584` (`rebuildAPIKeyModelAliasFromRuntimeConfig`): giant 6-arm provider switch (gemini/claude/codex/xai/vertex/openai-compat) with near-identical bodies. Replace with `map[providerKey]func(auth)*entry` and one call site. -50 lines.
L20. `sdk/cliproxy/auth/conductor_models.go:916` (`apiKeyModelAliasTable`): redefined type after `conductor_models.go:584` already builds one inline. One declaration. -3 lines.
L21. `sdk/cliproxy/auth/conductor_cooldown.go:46` (`quotaCooldownDisabledForAuthWithConfig`): 4-source override chain (auth override → provider override → global config → global atomic). One config source at a time: read `auth.Attributes` first, fall through to `cfg.DisableCooling`, done. -25 lines.
L22. `sdk/cliproxy/auth/conductor_cooldown.go:67` (`providerCoolingOverrideForAuth`): 4-level attribute resolution (provider_key, compat_name, openai-compatibility) feeding into one config lookup. Inline at the single caller; the function has one call site. -25 lines.
L23. `sdk/cliproxy/auth/conductor_cooldown.go:163` (`ApplyConfigWithCooldownStateStore`): 30-line atomic "old store → new store" handover with two ctx.Err() polls. Single function: `old = m.store; m.store = new; old.Save(ctx, snap)`. -20 lines.
L24. `sdk/cliproxy/auth/conductor_cooldown.go:131` (`SetConfigSnapshot` + `setConfigSnapshotLocked`): two functions for one thing; the `bool` return is only used to decide whether to persist. Inline. -25 lines.
L25. `sdk/cliproxy/auth/conductor_home.go:1` (`HomeDispatchBundle` + `PublishHomeDispatch` + `ClearHomeDispatchBundle` + legacy `SetHomeExecutionRegistry`/`ClearHomeExecutionRegistry`/`HomeExecutionRegistry`): atomic.Pointer dance for "store a (client, registry) pair". Three legacy shims that just call into the new bundle. Delete the legacy trio. -50 lines.
L26. `sdk/cliproxy/auth/conductor_home.go:96` (`homeErrorEnvelope` + `homeErrorDetail` + `homeDispatchRetryAfterError` + `homeRetryRoundExhaustedError` + `markHomeRetryRoundExhausted` + `isHomeRetryRoundExhausted` + `homeRetryRoundTiming`): 7 helper types for one retry-after field. Stdlib: `errors.New("retry round exhausted: " + reason)`, no struct. -100 lines.
L27. `sdk/cliproxy/auth/conductor_execution.go:50` (`upstreamExecutionAttemptError` + 6 helpers: `markUpstreamExecutionAttempt`, `markUpstreamExecutionAttemptFromContext`, `hasUpstreamExecutionAttempt`, `unwrapUpstreamExecutionAttempt`, `unwrapExecutionBoundaryError`, `preferredExecutionAttemptError`): 60 lines to flag an error as "actually hit upstream". `ctx.Err()` or a one-bit `bool` field on the existing `Error` struct covers it. -50 lines.
L28. `sdk/cliproxy/auth/conductor_execution.go:115` (`Execute`) and `ExecuteCount` (line 162) and `conductor_home_execution.go:24` (`executeHome`): 3 near-identical retry loops with subtle flag differences. One `executeWithRetry(ctx, mode, …)` with `mode` as a 3-state enum. -150 lines.
L29. `sdk/cliproxy/auth/conductor_execution.go:170` (`ExecuteCount`): copy-paste of `Execute` differing only in `executeCountMixedOnce` vs `executeMixedOnce`. Pure delegation. -120 lines.
L30. `sdk/cliproxy/auth/conductor_home_execution.go:43` (`executeHomeOnce`): 200-line function with 6 nested `for`/`if` blocks, 14 mutable locals, and 3 inline error-construction patterns. Standard "extract method" until each block is <30 lines. -100 lines.
L31. `sdk/cliproxy/auth/conductor_stream.go:23` (`streamBootstrapError` + `cloneHTTPHeader` + `newStreamBootstrapError` + `discardStreamChunks` + `streamErrorResult` + `validateStreamResult` + `readStreamBootstrap`): 7 helpers wrapping one HTTP error. Stdlib: just return the error with `%w`; `http.Header.Clone` already exists. -60 lines.
L32. `sdk/cliproxy/auth/conductor_refresh.go:65` (`queueRefreshReschedule` + `queueRefreshUnschedule` + 4 plumbing functions for the heap loop): 25 lines of "lock, lookup, signal, unlock". Inline at the 3 callers. -20 lines.
L33. `sdk/cliproxy/auth/conductor_refresh.go:170` (`authPreferredInterval` + `durationFromMetadata` + `durationFromAttributes`): 4-key × 2-source lookup loop for one int. `parseDurationValue(metadata["refresh_interval_seconds"])` directly. -25 lines.
L34. `sdk/cliproxy/auth/persist_policy.go:1` (whole file): 2-key context wrappers for `WithSkipPersist` / `WithDeferredAPIKeyModelAliasRebuild` — single bool in each. Use a 2-field `persistOptions` struct value in context, not two context keys. -10 lines.
L35. `sdk/cliproxy/auth/custom_headers.go:1` (whole file): `ExtractCustomHeadersFromMetadata` + `ApplyCustomHeadersFromMetadata` doing `map[string]string`/`map[string]any` two-case switch. Stdlib: `for k, v := range m { … }` once. -30 lines.
L36. `sdk/cliproxy/auth/credential_policy.go:1` (whole file): one constant + one normalizer + one switch over a single case. The constant is referenced in 2 files. YAGNI: delete the `credentialPolicy` concept and inline the codex-alpha-search boolean at the 1 call site. -40 lines.
L37. `sdk/cliproxy/auth/metadata_keys.go:1` (whole file): 12-entry alias table (`api-key` → `api_key`, etc.) for legacy config names. If a `models.json` from before the rename still uses the old keys, fail loud at parse. -30 lines.
L38. `sdk/cliproxy/auth/home_selection.go:1` (whole file): `executionResources` + `attemptCancels` + `HomeDispatchSelection` + 8 helper methods, ~280 lines of resource-lifetime tracking. `sync.WaitGroup` + `defer cancel()` covers the same surface. -200 lines.
L39. `sdk/cliproxy/auth/classification.go:1` (whole file): `AuthKind()` + `AuthSourceKind()` + `NormalizeAuthKind` + `NormalizeAuthSourceKind` for a 2-value enum. Stdlib: store the kind as a string, compare directly. -50 lines.
L40. `sdk/cliproxy/auth/oauth_model_alias.go:1` (whole file): "OAuth alias table" is a `map[channel]map[alias]entry`; rebuild-on-Set, atomic.Publish, query helper, and a "preserve suffix" path. A single `map[string]string` (channel.alias → upstream) is enough; resolve at register time. -100 lines.
L41. `sdk/cliproxy/auth/conductor.go:120` (`Manager.runtimeConfig` `atomic.Value`): 3 places do `Store(&Config{})` to satisfy "non-nil initial value". Use a plain `*Config` field guarded by `cfgMu`; drop the atomic. -10 lines.
L42. `sdk/cliproxy/auth/conductor_request_scoped_errors.go:1` (`requestStopError` + `IsRequestStop`/`Unwrap`/`wrapRequestStopError` + `extractRequestScopedErrorRules` 80-line `case` switch + `extractErrorBody` + `matchRequestScopedErrorAction`): full regex/status matching engine for "stop retry on this body". One `func stopOnError(auth, body) bool` and 1 regex per config row; read once at config time. -120 lines.
L43. `sdk/cliproxy/auth/types.go:153` (`Auth.Clone`) and `types.go:201` (`RecentRequestBucket` + `recentRequestRing` + 10-min bucket rotation): `recentRequests` ring is an in-memory counter used for one rate-limit hint. `sync/atomic.Int64` for success/failed is enough. -50 lines.
L44. `sdk/cliproxy/auth/types.go:73` (`Status` + 3 constants): status as a 3-value string. Drop the type, use plain string. -10 lines.
L45. `sdk/cliproxy/auth/conductor_lifecycle.go:23` (`SetRetryConfig`): three atomic-stored ints to express "retry config". One `retryConfig struct{…}` value behind `m.cfgMu`; expose `(r) *retryConfig` getter. -10 lines.
L46. `sdk/proxyutil/proxy.go:73` (`BuildHTTPTransport`) and `proxy.go:170` (`BuildDialer`): two near-identical Parse-then-dispatch functions, each with a SOCKS5/HTTP/HTTPS branch. One `Build(raw) (http.RoundTripper | proxy.Dialer, error)` returning an interface covers both, or two functions but no `Mode` return. -40 lines.
L47. `sdk/proxyutil/proxy.go:115` (`buildHTTPSProxyDialTLSContext`): hand-rolled CONNECT-over-TLS for "use HTTPS proxy". Stdlib `http.Transport{Proxy: http.ProxyURL(url)}` already handles `https://` proxy URLs since Go 1.20. -60 lines.
L48. `sdk/proxyutil/proxy.go:230` (`httpConnectDialer`): full custom CONNECT implementation. `golang.org/x/net/proxy` has `http.ProxyURL` (HTTP) and the `socks5` package handles SOCKS; only CONNECT-over-HTTPS needs a custom dialer. -100 lines.
L49. `sdk/proxyutil/proxy.go:30` (`Mode` enum + `Setting` struct): 4-state mode for "interpret a proxy string". One `bool useProxy + *url.URL` field is enough; the four modes collapse to `nil` (inherit) / `nil+direct` / `url`. -30 lines.
L50. `internal/logging/diagnostic.go:13` (`accessTokenExpiredLogPattern` + `sensitiveLogAssignmentPattern` + `authorizationLogPattern` + `urlUserinfoLogPattern` + `diagnosticStatusPattern`): 5 regexes for log redaction. Stdlib has no log-redaction helper, but a single `regexp.MustCompile(\`(?i)(token|secret|password|authorization|key|credential)[ =:]+[^\s,;]+\`)` covers the use case; one pattern. -25 lines.
L51. `internal/logging/diagnostic.go:40` (`SafeErrorDiagnostic`): 25-entry `signal{needle,label}` table doing `strings.Contains` over a lower-cased error message. The existing 5-line `errors.Is(err, io.EOF)` block at the top already catches the real cases; the substring table is a second classifier that disagrees with the type-based one. Delete the substring table. -45 lines.
L52. `internal/logging/diagnostic.go:130` (`truncateDiagnosticLogExcerpt`) + `diagnosticRunePrefix`: hand-rolled rune-aware prefix + suffix-keep-marker. Stdlib: `utf8.RuneCountInString` + slice + `if marker outside window { append }` is 5 lines. -25 lines.
L53. `internal/logging/diagnostic.go:55` (`appendPart`): a `parts []string` with manual dedup. `map[string]struct{}` or just sort+unique at the end. -10 lines.
L54. `internal/registry/model_registry.go:60` (`ModelInfo`): 22 fields, 17 of them `json:"…,omitempty"`. Several (`UserDefined`, `IsCompat`, `MaxContextLength`) are `json:"-"` "internal metadata" never read outside the file. Delete the unused ones. -15 lines.
L55. `internal/registry/model_registry.go:120` (`ModelRegistration`): 7 fields, 4 with `map[string]*time.Time` / `map[string]string` for tracking "quota exceeded" / "suspended" clients. Replace with `[]string` slices + one map for "clientID → reason" if any of this is actually read. -25 lines.
L56. `internal/registry/model_registry.go:160` (`ModelRegistry`): 12-field struct, 5 maps all keyed by clientID, two of which (`clientEpochs`, `clientGenerations`) are monotonic counters only used to invalidate the cache. Drop the cache: re-render on each `GetAvailableModels` call (it's a 10-line iteration). -60 lines.
L57. `internal/registry/model_registry.go:200` (`triggerModelsRegistered` + `triggerModelsUnregistered`): goroutine-launched hook with 5s timeout and panic recovery for "tell observer we changed". `hook.OnModelsRegistered(ctx, …)` synchronous, panic-recovered at the boundary. -30 lines.
L58. `internal/registry/model_registry.go:260` (`RegisterClient`): 100-line function with 4 add/remove cases (`pure addition`, `provider changed`, `mixed`, `pure removal`). The "diff old vs new models" logic is reimplementing `slices.DeleteFunc`. -60 lines.
L59. `sdk/cliproxy/executionregistry/registry.go:1` (whole file, ~470 lines): full `BeginDispatch` → `Install` → `End` → `EndWithRelease` → `SetReleaseSink` state machine with `ReleaseGroup`/`ReleaseTicket`/`Scope` for "track a Home execution". `chan struct{}` per execution + a `sync.WaitGroup` is the same surface. -350 lines.
L60. `sdk/cliproxy/executionregistry/registry.go:42` (`PendingDispatch` + `Scope` + `ScopeSpec` + `ReleaseGroup` + `ReleaseTicket`): 5 types for one integer dispatch ID. `type dispatchID uint64` and a `map[uint64]context.CancelFunc` is enough. -100 lines.
L61. `sdk/cliproxy/executionregistry/registry.go:130` (`SetReleaseSink` `any` switch with 4 case types): accepts a function or `ReleaseSink` or both. One signature. -15 lines.
L62. `internal/runtime/executor/antigravity_reasoning_replay.go:42` (`antigravityCountClaudeToolProvenanceIDs`): recursively counts `functionCall.id`/`functionResponse.id` matches inside a Gemini-shaped JSON. `strings.Count(string(payload), prefix)` on the first 1KB is a 1-line approximation. -25 lines.
L63. `internal/runtime/executor/antigravity_reasoning_replay.go:75` (`antigravityReasoningReplayScopeFromPayload`): 3-step fallback ("explicit sessionID" → "stable hash" → ""). Stdlib `hash/fnv` of the body in one line. -15 lines.
L64. `internal/runtime/executor/claude_thinking_replay.go:33` (`claudeThinkingReplayModelFamily`): `sha256.Sum256` + `hex.EncodeToString` for a "model family" tag. `auth.ID + ":" + baseModel` is enough (collisions are fine, cache miss just means recompute). -10 lines.
L65. `internal/runtime/executor/claude_signing.go:21` (`claudeCCHJSONScanner` + 3 edit/member types): hand-rolled JSON whitespace normalizer for a single 5-digit token insertion. `bytes.ReplaceAll` of the `00000` placeholder and `regexp.MustCompile` to find `cch=XXXXX` covers it. -80 lines.
L66. `internal/runtime/executor/claude_signing.go:13` (`claudeCCHUpstreamKind` enum + 3 constants): one bit of context ("are we on Anthropic or Vertex"). `bool isVertex` parameter. -10 lines.
L67. `internal/runtime/executor/openai_responses_signature.go:55` (`stripOrphanReasoningIDs` flag via `gjson.GetBytes` then 3-line rewriter): a single `sjson.DeleteBytes` call on the items array, no custom rebuild loop. The "backfill preceding items" approach (`startRebuild`) is what `sjson.DeleteBytes(body, "input.3.id")` does in one call. -30 lines.
L68. `internal/runtime/executor/openai_compat_executor.go:33` (`openAICompatMultipartMemory` = 32MB const) and the multipart upload path: hand-rolled `mime/multipart` for image edits. Most image-edit providers accept base64; check if a base64 path is even needed. -50 lines.
L69. `internal/runtime/executor/claude_fingerprint_policy.go:30` (`claudeFingerprintPolicy` 9-bool struct): "what fingerprint to apply" encoded as 9 separate bools, then translated into 9 separate decisions. One `Profile` enum (`Default` / `ClaudeCodeCLI`) and a `case` per profile at the single consumer. -30 lines.
L70. `internal/runtime/executor/claude_fingerprint_policy.go:46` (`claudeFingerprintProfileWarned sync.Map` + `LoadOrStore` per call): "warn-once" via a map keyed by the unrecognized value. The map is bounded by the number of bad config values ever seen; a plain `log.Warn` once on startup covers the same thing. -10 lines.
L71. `internal/runtime/executor/openai_compat_executor.go:80` (`PrepareRequest` + `resolveCredentials` + `httpClient.Do`): 3-step request execution where `httpClient.Do` already accepts the auth. The `PrepareRequest` indirection is only used here. Inline. -20 lines.
L72. `sdk/cliproxy/builder.go:25` (`Builder` with 6 fields and 5 `With*` setters): builder pattern for a struct that's set up in one place (`service.go`). Replace with a struct literal at the call site. -40 lines.
L73. `sdk/cliproxy/service.go:55` (`Service` struct, 28 fields including 4 home mutexes + 4 home hooks + 3 home lifecycle fields): home state is its own concern. `type serviceHome struct {…}` and embed. -40 lines.
L74. `sdk/cliproxy/service.go:50` (`pprofServer` + `homeSupervisor` + `homeLogForwarder` + `homePluginSyncFetch` + `homePluginDeleteTask`): 5 hooks for "configure-able behavior on this lifecycle event". One `HomeHooks` struct. -30 lines.
L75. `sdk/cliproxy/service.go:23` (`configSequence uint64` + `appliedRoutingState *routingRuntimeState`): monotonic counter + state pointer for "did the config change?" — re-read `m.cfg` under `m.cfgMu` and compare a `version int` field on the config. -15 lines.

## Net estimate

Top-impact cuts: scheduler (1000), reasoning replay scope (50), cooldown state file (180), session alias LRU (110 + 55), home in-flight publisher (120), auto-refresh heap (200), executionregistry (350), home_selection (200), conductor helpers (300), proxyutil CONNECT (160), diagnostic (105), model registry cache (90).

Conservative: -3500 lines possible. Aggressive: -5500.

net: -3500 lines possible.