# 2026-08-01 工作日志

## 代码审核报告修复 — 第一批（安全/崩溃类）

针对项目主链路核心代码质量审核报告，按修复路线图实施第一批修复（5 项，全部完成并通过编译/测试/vet）。

### 修复清单
1. **P0 并发崩溃** — `internal/api/handlers/management/config_lists.go`：`GetAPIKeys`/`GetOAuthExcludedModels`/`GetOAuthModelAlias` 三个 GET handler 读取前加 `h.mu.Lock()` 并拷贝响应数据，与写端锁对齐，消除 `concurrent map read and map write` 致命崩溃。
2. **P0 浅拷贝竞态** — `config_basic.go` `GetConfig`：原 `new(*h.cfg)` 做浅拷贝（map header 共享），改为 `h.mu.Lock()` + `CloneForRuntime()`（深度克隆）+ 解锁后序列化。实证 `new(*f)` 产出非零副本值，证实浅拷贝行为。
3. **P1 X-Forwarded-For 伪造** — `internal/api/server.go` `NewServer`：`gin.New()` 后显式 `engine.SetTrustedProxies(nil)`，使 `ClientIP()` 回退 TCP 对端地址，防远程伪装本机绕过管理禁用闸门。
4. **P1 nil map panic** — `internal/runtime/executor/kimi_executor.go`：新增 `setKimiBaseURLAttr(auth)` helper（判空 auth + 判空初始化 Attributes），替换 :88/:198/:339 三处裸赋值。
5. **P1 刷新楔死** — `antigravity_executor_auth.go` `refreshTokenSingleFlight`：加 `context.WithTimeout(ctx, 30s)` 用于 OAuth 刷新请求，`WithoutCancel` 仅保留给回写，避免 singleflight 永久楔死。
6. **P1 硬编码 secret** — `internal/auth/antigravity/constants.go`：`ClientSecret` 由 const 改为 env 覆盖的 var（`ANTIGRAVITY_OAUTH_CLIENT_SECRET`，默认值保留并注明公开属性）；同时删除 executor 与 management 的重复常量定义，统一引用中央 `antigravity.ClientID`/`antigravity.ClientSecret`（消除三处重复）。

### 验证
- `go build ./...` + `go build -o test-output ./cmd/server` 通过
- `go test`：auth/antigravity、management、runtime/executor、api 全部 ok
- `go vet` 受影响包干净；`gofmt -l` 干净

### 待办
- 第二批（功能正确性）：codex WS incomplete、reasoning_content 字段、缓存降级、wsrelay 背压、P2 #1/8/9/10/16/17
- 第三批：其余 P2 + 全部 P3

### 关键认知
- `withAuthIndex`（config_auth_index.go:102）已加锁，是既有安全读模式；被标记的 4 个 GET handler 是漏锁者。
- `internal/auth/antigravity/constants.go` 是 OAuth client 凭据的规范集中处，executor/management 此前各自重复定义。
- AGENTS.md：credential acquisition 阶段允许设超时；antigravity 刷新属此范畴。

## 代码审核报告修复 — 第二批（功能正确性 P2 #1/#8/#9/#10/#16/#17）

第二批全部完成并通过 `go build ./...`、服务端二进制编译、`gofmt -l`、`go vet`、`go test`（新测试额外 `-race`）。

### 修复清单
1. **P2 #1 thinking null 守卫** — `internal/thinking/apply.go`：新增 `fieldNonNil`（Exists && Type!=Null），应用到 extractClaude/Gemini/OpenAI/Codex 四个配置提取函数，避免 `null` 字段被读成 0/"" 误触发思考模式。测试 `apply_null_guard_test.go`。
2. **P2 #9 claude cloaking 无 user message 丢指令** — `claude_executor_cloaking.go`：指令折叠进初始 system 数组的一次 `sjson.SetRawBytes`（避免二次 raw 写入被 `injectFakeUserID` 重序列化丢弃）；`prependToFirstUserMessage` 改返回 `([]byte, bool)`。测试 `TestApplyCloaking_PreservesSystemInstructionsWhenNoUserMessage`。
3. **P2 #10 antigravity 429 丢弃 retryAfter** — `antigravity_executor_credits.go`：非 `RESOURCE_EXHAUSTED` 的 429 若解析出 `retryAfter` 则改 `instantRetrySameAuth`。两个测试覆盖有/无 retryAfter。
4. **P2 #16 translator 包级可变全局** — `claude/openai/chat-completions/claude_openai_request.go`：删除包级 `user`/`account`/`session`，改为每请求 `uuid.NewRandom()` 生成（消除并发数据竞争 + 每请求隔离缺陷）。测试 `TestConvertOpenAIRequestToClaude_DistinctUserIDPerCall`（`-race`）。
5. **P2 #17 OpenAI→Claude SSE usage 门控 + [DONE] 终止** — `openai/claude/openai_claude_response.go`：usage 跨 chunk 累积于 `param`，当 stop_reason 与 usage 就绪发射唯一 `message_delta`，或 `[DONE]` 时 `emitMessageDeltaWithUsage` 无条件补发。测试 `TestStreaming_UsageBeforeFinishReason_EmitsBoth` / `TestStreaming_DoneWithoutFinishReason_Terminates` + 既有 `TestStreaming_LateUsageOnlyDoesNotEmitAfterMessageStop`。

### 误报（未改动）
- **P2 #8 xai reasoning replay 幽灵回合**：经核实为合法多轮回放（客户端每轮只发最新 user message，代理须从缓存重建历史）。加守卫会破坏 `TestXAIExecutorResponsesSSEReplaysEncryptedReasoningAndAssistantMessage`，已回退。报告条目系误读。

### 关键认知
- `gjson.Exists()` 对 JSON `null` 返回 true；区分 null 与缺失必须用 `Type != gjson.Null`。
- `sjson.SetRawBytes` 与后续 `sjson.SetBytes` 混用会触发整 payload 重序列化，丢弃前一次 raw 写入的块——cloaking 中须把多块折叠进单次 raw 写入。
- antigravity 429 决策：`decideAntigravity429` 解析 `Retry-After`（`ParseRetryDelay` 读 `error.details[].RetryInfo.retryDelay`）；`RESOURCE_EXHAUSTED` 走配额逻辑，其余 429 尊重服务端 retryAfter。
- 变更摘要见 `BATCH2-CHANGE-SUMMARY.md`。

### 待办
- 第三批：P2 #2–7, #11–15, #18–26 及全部 P3。

## 代码审核报告修复 — 第三批（边界与清理）

### 已完成（截至本会话）
- **thinking 包（Task #15）**：P2 #2–5 + P3 五项已完成。
- **executor 包（Task #19）**：P2 #6/#7/#11 + P3 八项已完成；`codexStreamReadError`（502, request-scoped）区分"读失败"与"流读完无终止事件"（408）；utls 连接建立仅对 dial 设 `utlsConnectTimeout`，符合 AGENTS.md 超时例外。
- **api 包（Task #16）全部完成**：P2 #12 ZAIKey 漏分支（config_apikey_disable.go 用 `"zai:apikey"` kind）；#13 trim 不对称（deleteFromStringList 精确匹配）+ 死 map 删除；#14 SSRF 三道防线（scheme 白名单/CheckRedirect 拒内网/限响应体）；#15 路径逃逸（见下）；P3 withAuthIndex TOCTOU、normalizeOpenAICompatibilityEntry 死 map、server_reload ZAIKey 计数、request_logging Authorization/X-Management-Key 掩码（扩展 `util.MaskSensitiveHeaderValue` 识别 `x-management-key`，同步保护 request_logger_format.go / logging_helpers.go 两处调用方）。

### 关键修复与认知
- **P2 #15 deleteAuthFileByName 路径逃逸（修正）**：第一版实现把"路径逃逸防护"做成了拒绝所有 AuthDir 之外的文件，但设计上**允许** managed auth record 的 `path` 属性指向 AuthDir 之外（测试 `TestDeleteAuthFile_UsesAuthPathFromManager` 把真实文件放在 externalDir 并要求被删）。正确做法：对**未受信任的 name 入参**解析出的路径做 `authDirContains` 校验（defense-in-depth，且 `filepath.Base(name)` 已剥离 `../`），但仍**尊重** stored record `path` 覆盖。已修复并通过全部 4 个 TestDeleteAuthFile_* 测试。
- **预存 flaky 测试**：`TestDeletePluginRemovesDiscoveredFileAndConfig`（`handlers/management`）在整包负载下偶发超时（1s 等 `DeletePlugin` 异步 reload 完成），独立运行稳定通过。该路径（plugins.go → reloadConfigAfterManagementSaveAsync）未被本批次任何改动触及，属既有 flake，非本次引入。
- **git 仓库损坏**：`.git` 的 tree object 损坏（`unable to read tree`），`git stash`/`git diff` 均不可用，无法做基线比对。改动均保留在磁盘工作区（前序批次亦未提交）。需留意：后续验证只能靠 `go build`/`go test`，不能依赖 git。

### 第三批收尾（已全部完成）
- **wsrelay（#18）**：`cleanup()` 改 Range 内逐键 Delete（消除 `s.pending = sync.Map{}` 替换竞态）；`pendingRequest` 加 `done chan`，监控 goroutine `select` 增 `case <-req.done` 消除泄漏；`run(ctx)` → `run()`。
- **registry（#21 P2 #24/#25）**：新增 `preserveMissingSections(oldData,newData)`，刷新目录中为空而现役非空的 section 沿用现役定义（在写 store、在 `detectChangedProviders` 之前调用），修掉"远程缺 section 静默清空"；`getModels()` 改为永不返回 nil（store 空时返回 `emptyModelsCatalog` + `sync.Once` 告警），一处收口覆盖 14 个 `Get*Models` 与 `LookupStaticModelInfo` 的 nil 解引用 panic。
- **signature P3**：`gpt_validation.go` 的 `gAAAA` 前缀检查前移到全串扫描/解码之前。**grok 未动**——其 Claude/Gemini 交叉探测必须早于本地 decode，`TestInspectGrokEncryptedContent_Rejects*` 显式断言 "Claude"/"Gemini" fast-reject 错误串，重排会破坏契约。
- **摘要产出**：`BATCH3-CHANGE-SUMMARY.md`（45 项逐项状态 + 验证表 + 范围外发现）。

### 范围外发现与修复（重要）
- **cache 淘汰不确定性（真 bug，已修）**：`evictOldestAntigravityReasoningReplayEntries` 与 `reasoningReplayStore.evictOldestLocked` 用 `sort.Slice`（不稳定）且只按 timestamp 比较；Windows 时钟粒度粗使微秒内写入的条目 timestamp **完全相同**，淘汰目标遂由 Go 随机 map 迭代顺序决定 → `TestAntigravityReasoningReplayUnrelatedEvictionDoesNotBlockAbsentSnapshot` 同代码交替通过/失败。两处加 key 字典序 tie-break 后连续 6 轮全绿。**教训：按时间戳排序做淘汰必须带确定性 tie-break。**
- **codex 缓存测试契约过期（已修）**：第二批把 `expireFailureFatal` 设 false，xai 侧测试已迁移（`TestXAIReasoningReplayRequiredHomeExpireFailureReturnsItems`）但 codex 侧遗漏，导致 `TestCodexReasoningReplayRequiredHomeFailures/expire` 稳定失败。已按 xai 模式重写。
- **`go vet ./...` 既有告警（未修，建议下一批）**：`internal/pluginhost/host_callbacks.go:164,178` 与 `host_model_stream_callbacks.go:29,48` 的 **cancel 未在所有路径调用（context 泄漏）**，与 P2 #23 同类；另有 `internal/logging` WriteTo 签名、`loader_windows.go` unsafe.Pointer ×8。均在本批未触及的包中。
- **`go build` 需加 `-buildvcs=false`**：git tree 损坏导致 main 包 VCS 打戳失败（`error obtaining VCS status: exit status 128`），非代码问题。

### 权限边界澄清（AGENTS.md）
- 第二批 #16/#17 同样改了 translator 却合规：那批是横跨 thinking/executor/translator 的**整体**改动，命中"may modify it only as part of broader changes elsewhere"豁免。
- 第三批 Task #17 是**纯 translator** 改动，不满足豁免，READ 权限下必须停止并提 issue（#4720）。

### 待办
- translator 11 项：issue #4720 已由用户决定**关闭**（state=CLOSED, reason=NOT_PLANNED，关闭时附评论说明"无 translator 写权限、分析与补丁保存在 TRANSLATOR-AUDIT-ISSUE.md，有写权限者可直接开 PR"）。修复内容仍未落地，唯一载体是本地 `TRANSLATOR-AUDIT-ISSUE.md`——**别删这个文件**。
- 建议下一批：`internal/pluginhost` context 泄漏。

### Translator 批次被权限阻断（关键）
- `gh repo view router-for-me/CLIProxyAPI --json viewerPermission` 返回 **READ**。AGENTS.md 规定：改动 `internal/translator/` 需仓库 WRITE/MAINTAIN/ADMIN；仅 READ 时**禁止改 translator**，须提 GitHub issue 含 goal/rationale/intended code 后停止。
- 已按规提 issue：**https://github.com/router-for-me/CLIProxyAPI/issues/4720**，内容含 11 项（P2 #18–21/#26 + P3 #27–31）的 file/行号/bug/intended code 草图。本地另存 `TRANSLATOR-AUDIT-ISSUE.md`。
- 因此 Task #17 代码改动**未落地**，仅完成调研+issue。后续需具 WRITE 权限的维护者合入。
- translator 内所有目标符号已定位：custom_tool_call（`openai_openai-responses_response.go` + `openai_openai-responses_tools.go:32`）、tool_result 数组降级（`interactions/claude/interactions_claude_request.go:216`）、非流式首 part（`claude_openai-responses_response.go:195`、gemini 同 ~296）、块索引（`codex/claude/codex_claude_response.go:630`、`openai/claude/openai_claude_response.go:512`）、fixCLIToolResponse FIFO 错配（`antigravity/gemini/antigravity_gemini_request.go:554`）、ToolCallAccumulator 三重复制、data: 前缀 4 处、`reasoning_summary_text.done` 硬编码换行。
